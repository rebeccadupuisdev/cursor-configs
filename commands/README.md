# Cursor Commands — Naming & Creation Guide

Cursor commands are **shorthand triggers** for activating personas, rules, or behavior patterns within a session. They make it easy to switch modes without editing configuration files.